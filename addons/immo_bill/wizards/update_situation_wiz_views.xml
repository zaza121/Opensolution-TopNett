<odoo>

    <record id="view_update_situation_wiz" model="ir.ui.view">
        <field name="name">Update situation Wizard</field>
        <field name="model">immo_bill.update_situtaion_wiz</field>
        <field name="arch" type="xml">
            <form string="Update situation wizard">
                <p class="oe_grey">
                    Invoices will be created in draft so that you can review
                    them before validation.
                </p>
                <group>
                    <group>
                        <field name="situation_order" />
                        <field name="immo_bill_id" invisible="1" />
                        <field name="date_confirmation" />
                    </group>
                    <group>
                    <!-- <group name="down_payment_specification"
                        attrs="{'invisible': [('advance_payment_method', 'not in', ('fixed', 'percentage'))]}"> -->
                        <field name="situation_progress" widget="percentage" />
                        <field name="final_progress" widget="percentage" string="Final situation"/>
                        <field name="amount" />
                        <field name="initial_amount" invisible="1"/>
                        <!-- <field name="company_id" invisible="1"/>
                        <field name="product_id" invisible="1"/>
                        <label for="amount"/>
                        <div id="payment_method_details">
                            <field name="currency_id" invisible="1"/>
                            <field name="fixed_amount"
                                attrs="{'required': [('advance_payment_method', '=', 'fixed')],
                                    'invisible': [('advance_payment_method', '!=', 'fixed')]}"
                                class="oe_inline"/>
                            <field name="amount"
                                attrs="{'required': [('advance_payment_method', '=', 'percentage')],
                                    'invisible': [('advance_payment_method', '!=', 'percentage')]}"
                                class="oe_inline"/>
                            <span attrs="{'invisible': [('advance_payment_method', '!=', 'percentage')]}"
                                class="oe_inline">%</span>
                        </div>
                        <field name="deposit_account_id"
                            options="{'no_create': True}"
                            attrs="{'invisible': [('product_id', '!=', False)]}"
                            groups="account.group_account_manager"/>
                        <field name="deposit_taxes_id"
                            widget="many2many_tags"
                            attrs="{'invisible': [('product_id', '!=', False)]}"/> -->
                    </group>
                </group>
                <footer>
                    <button name="action_update" type="object"
                        id="create_invoice_open"
                        string="Update Situation"
                        class="btn-primary" data-hotkey="q"/>
                    <!-- <button name="create_invoices" type="object"
                        id="create_invoice"
                        string="Create Invoice"
                        context="{'open_invoices': True}"
                        data-hotkey="w"/> -->
                    <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="z"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_view_sale_advance_payment_inv" model="ir.actions.act_window">
        <field name="name">Update Situation</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">immo_bill.update_situtaion_wiz</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="immo_bill.model_immo_bill_invoice" />
        <field name="binding_view_types">list</field>
    </record>

</odoo>