<odoo>

	<data noupdate="1">

		<!--Email template for situation invoice -->
        <record id="email_template_immo_invoice" model="mail.template">
            <field name="name">Situation invoice: Sending</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="email_from">{{ (object.invoice_user_id.email_formatted or user.email_formatted) }}</field>
            <field name="partner_to">{{ object.partner_id.id }}</field>
            <field name="subject">{{ object.company_id.name }} Invoice (Ref {{ object.name or 'n/a' }})</field>
            <field name="description">Sent to customers with their situation invoices in attachment</field>
            <field name="body_html" type="html">
<div style="margin: 0px; padding: 0px;">
    <p>Bonjour,</p>
    <p><br/></p>
    <p>Nous vous prions de bien vouloir trouver ci-joint notre note d’honoraires concernant
l’affaire référencée ci-dessus.</p>
	<p><br/></p>
	<p>Vous en souhaitant bonne réception,</p>
	<p><br/></p>
	<p>Cordialement,</p>
	<p><br/></p>
	<p style="margin: 0px; padding: 0px; font-size: 13px;"></p>
</div>
            </field>
            <field name="report_template" ref="immo_bill.situation_invoice_report_action"/>
            <field name="report_name">Invoice_{{ (object.name or '').replace('/','_') }}{{ object.state == 'draft' and '_draft' or '' }}</field>
            <field name="lang">{{ object.partner_id.lang }}</field>
            <field name="auto_delete" eval="True"/>
        </record>

	</data>

</odoo>